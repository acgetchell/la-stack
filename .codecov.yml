---
coverage:
  status:
    project:
      default:
        target: 98%
        removed_code_behavior: removals_only
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 2%

ignore:
  - "src/lib.rs"
  - "benches/**"
  - "examples/**"
