# Clippy configuration for the la-stack crate.

# Keep lint behavior aligned with Cargo.toml and rust-toolchain.toml.
msrv = "1.96.0"

# Lint levels remain owned by Cargo.toml and the canonical just recipes.
